---

nullmailer_mailname: '{{ ansible_fqdn }}'
nullmailer_idhost:   '{{ ansible_fqdn }}'
nullmailer_remotes:

nullmailer_adminaddr:

# These are rarely used:
nullmailer_helohost:
nullmailer_pausetime:
nullmailer_sendtimeout:

# setup smtp via xinetd
nullmailer_smtpd: True
nullmailer_smtpd_port: 25
nullmailer_smtpd_host: localhost
